import { FiltroEstablecimientoPipe } from './filtro-establecimiento.pipe';

describe('FiltroEstablecimientoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEstablecimientoPipe();
    expect(pipe).toBeTruthy();
  });
});
